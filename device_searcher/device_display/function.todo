# Todo 添加管理页面，可以调用抓取模块和更新函数，并显示进度
# Todo 添加点击型号中的品牌/设备类型，浮动显示详细信息
# Todo 添加搜索结果导出模块，支持导出多种格式，包括导出到文件，或者在页面中生成json格式字符串
# 修复设备类型识别问题（如何将不同字符串，但表示相同类型，识别为同一个类型，从而避免每条记录都进行翻译请求）
# 记录下因为设备类型英文翻译已存在导致的插入记录失败
# Todo 修复设备类型英文名太长而无法存储的问题